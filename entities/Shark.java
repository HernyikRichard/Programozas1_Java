package entities;

/**
 * Shark osztály a Entiti-ból számazik, a Shark objektumnak a müködése automatikus ön vezérelt
 */
public class Shark extends Entiti {
    public Shark(int x, int y) {
        super(x, y);
    }
}
